[project]
name = "se-labs-python"
version = "0.1.0"
description = "Personal SE Labs for GenAI skills and evals"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E","F","I","B"]
ignore = []

[tool.ruff.format]
quote-style = "double"
